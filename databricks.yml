bundle:
  name: playwright-mcp-on-apps

sync:
  include:
    - .build

artifacts:
  default:
    type: whl
    path: .
    build: uv build --wheel

resources:
  apps:
    playwright-mcp-on-apps:
      name: "playwright-mcp-on-apps"
      description: "Playwright MCP Server on Databricks Apps - Browser automation via Model Context Protocol"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
